let express,readdirSync,mongoose,cors;_de4‍.w("express",[["default",["express"],function(v){express=v}]]);_de4‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_de4‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_de4‍.w("cors",[["default",["cors"],function(v){cors=v}]]);

// import router from './routes/auth';
require("dotenv").config();



const morgan = require('morgan');
const app = express();

app.use(cors());
app.use(express.json());
app.use(morgan("dev"));
readdirSync("./routes").map((r) => {
    app.use('/api', require(`./routes/${r}`))
});

mongoose.connect(process.env.DATABASE).then(() => console.log("Db connected")).catch(() => console.log("DB Connection error"));


const port = 8001;
app.listen(port, () => console.log("Listening to the port"));